//package tanyapetrolpump.models.Pump;

/**
 *
 * @author Tanya Jain <https://tanya-jain.xyz>
 */
/*
public class Pump extends Lane {
    private final int pumpId = 1;
    private boolean pumpInUse;

    // Unique pump number
    private int pumpNumber;

    // Capability of pump to dispense fuel in litres per milisecond.
    private float dispensingCapability = 1.5/1000

    //Time taken in miliseconds by the pump to fuel a vehicle 
    private float fuellingProcessDuration = 8000

    // CONSTRUCTOR
    public Pump() {
        pumpNumber = pumpId++;
        pumpInUse = false;
    }
        
    // Setters
    public setPumpUsage( ) {
        pumpInUse = true;
        setLaneUsage();
    }

    // Getters
    public getPumpUsage( ) {
        return pumpInUse;
    }

    // METHODS
    public static void sendToLane( Vehicle vehicleArrivedOnALane ) {
        Pump emptyPump = new Pump;
        if pump[0].getLaneUsage() = false;
            emptyPump = pump[0];
        else if pump[1].getLaneUsage() = false;
            emptyPump = pump[1];
        else if lane[2].getLaneUsage() = false;
            emptyPump = pump[2];
        else 
            laneWaitQueue++;
        emptyPump.sendToLane( vehicleArrivedOnALane );
    }   
}
*/